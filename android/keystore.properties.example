# Example signing config. Copy to keystore.properties and fill in.
# DO NOT commit keystore.properties — it is in .gitignore.

SIGNING_KEYSTORE_PATH=release.keystore
SIGNING_KEY_ALIAS=anyfn
SIGNING_STORE_PASSWORD=changeme
SIGNING_KEY_PASSWORD=changeme
